#!/bin/bash

source $swarmConfigs/hornet.cfg

if [ "$hornetNetwork" = "mainnet" ]; then
    hornetConfig="config"
fi

if [ "$hornetNetwork" = "comnet" ]; then
    hornetConfig="config_comnet"
fi

if [ "$hornetNetwork" = "devnet" ]; then
    hornetConfig="config_devnet"
fi