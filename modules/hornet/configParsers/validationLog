#!/bin/bash

parser="$(jq '.receipts.backup.enabled' $hornetDir/config.json)"
if [ "$parser" != "$hornetValidationLog" ]; then
    sudo jq '.receipts.backup.enabled = '$hornetValidationLog'' $hornetDir/config.json|sponge $hornetDir/config.json
    restartHornet=true
    newValidationLog=true
fi