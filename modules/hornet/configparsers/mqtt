#!/bin/bash

parser="$(jq '.host' $hornetdir/mqtt_config.json)"
if [ "$parser" != "\"0.0.0.0\"" ]; then
    sudo jq '.host = "0.0.0.0"' $hornetdir/mqtt_config.json|sponge $hornetdir/mqtt_config.json
fi

