#!/bin/bash

parser=$(sed -n '/max_unknown_peers/p' /$beeDir/config.toml | awk '{ print $3 }')
if [ "$parser" != "$beeMaxUnknownPeers" ]; then
    sudo sed -i 's~^alias.*~alias      = "'$beeAlias'"~g' $beeDir/config.toml
    restartBee=true
fi