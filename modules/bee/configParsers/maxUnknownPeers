#!/bin/bash

parser=$(sed -n '/max_unknown_peers/p' $beeDir/config.toml | awk '{ print $3 }')
if [ $parser -ne $beeMaxUnknownPeers ] > /dev/null 2>&1; then
    sudo sed -i 's~^max_unknown_peers.*~max_unknown_peers       = '$beeMaxUnknownPeers'~g' $beeDir/config.toml
    restartBee=true
fi