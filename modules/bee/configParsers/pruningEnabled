#!/bin/bash

parser=$(sed -n '/enabled/p' $beeDir/config.toml | awk '{ print $3 }')
if [ "$parser" != "$beePruningEnabled" ]; then
    sudo sed -i 's~^enabled.*~enabled         = '$beePruningEnabled'~g' $beeDir/config.toml
    restartBee=true
fi