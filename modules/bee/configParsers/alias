#!/bin/bash

parser=$(sed -n '/alias/p' $beeDir/config.toml | head -n 1 | awk '{ print $3 }')
if [ "$parser" != "\"$beeAlias\"" ]; then
    sudo sed -i 's~^alias.*~alias      = "'$beeAlias'"~g' $beeDir/config.toml
    restartBee=true
    newBeeAlias=true
fi