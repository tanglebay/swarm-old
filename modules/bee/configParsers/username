#!/bin/bash

parser=$(sed -n '/user/p' /$beeDir/config.toml | awk '{ print $3 }')
if [ "$parser" != "\"$beeUsername\"" ]; then
    sudo sed -i 's~^user.*~user            = "'$beeUsername'"~g' $beeDir/config.toml
    restartBee=true
fi