#!/bin/bash

parser=$(sed -n '/delay/p' $beeDir/config.toml | awk '{ print $3 }')
if [ "$parser" != "$beePruningDelay" ]; then
    sudo sed -i 's~^delay.*~delay           = '$beePruningDelay'~g' $beeDir/config.toml
    restartBee=true
fi