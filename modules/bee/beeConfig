#!/bin/bash

source $swarmConfigs/bee.cfg

if [ "$beeNetwork" = "mainnet" ]; then
    beeConfig="config.chrysalis-mainnet"
fi

if [ "$beeNetwork" = "comnet" ]; then
    beeConfig="config.chrysalis-comnet"
fi

if [ "$beeNetwork" = "devnet" ]; then
    beeConfig="config.chrysalis-devnet"
fi