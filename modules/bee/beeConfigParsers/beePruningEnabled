#!/bin/bash

parser=$(sed -n '/^enabled/p' $beeHome/config.chrysalis-$beeNetwork.toml | awk '{ print $3 }')
if [ "$parser" != "$beePruningEnabled" ]; then
    sudo sed -i 's~^enabled.*~enabled         = '$beePruningEnabled'~g' $beeHome/config.chrysalis-$beeNetwork.toml
    restartBee=true
fi