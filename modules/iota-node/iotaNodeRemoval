#!/bin/bash

if [ "$iotaNodeRemoval" = "true" ]; then
    ( cd $iotaNodePath ; docker compose -f docker-iota.yml down > /dev/null 2>&1 )
    if [ -d $iotaNodePath ]; then
        rm -rf $iotaNodePath > /dev/null 2>&1
    fi
fi