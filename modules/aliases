#!/bin/bash

if [ ! -f "/etc/profile.d/00-aliases.sh" ]; then
    echo "alias swarm='sudo /var/lib/swarm/swarm'" > /etc/profile.d/00-aliases.sh
    echo "alias swarm='sudo /var/lib/swarm/swarm'" >> /etc/bash.bashrc
    restart=true
fi