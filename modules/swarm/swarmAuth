#!/bin/bash

if [ "$swarmAuth" != "200" ]; then
    swarmAuth=$(curl --max-time 5 -Ls -o /dev/null -w "%{http_code}" https://$swarmAuthUser:$swarmAuthPwd@$cdnUrl/test.file)
fi

if [ "$swarmAuth" = "200" ]; then
    swarmAuthText="OK [✓]"
else
    swarmAuthText="Unauthorized [X]"
fi