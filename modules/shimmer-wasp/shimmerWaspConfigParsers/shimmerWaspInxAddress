#!/bin/bash

parser=$(jq '.l1.inxAddress' $shimmerWaspHome/config.json)
parser=$(echo $parser | tr -d '"')
if [ "$parser" != "localhost:9030" ]; then
    sudo jq '.l1.inxAddress = "localhost:9030"' $shimmerWaspHome/config.json|sponge $shimmerWaspHome/config.json
    restartShimmerWasp=true
fi