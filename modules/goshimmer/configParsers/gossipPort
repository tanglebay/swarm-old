#!/bin/bash

if [ $goshimmerGossipPort -eq $goshimmerGossipPort ] && [ "$goshimmerGossipPort" -gt 0 ] 2>/dev/null; then
    sudo jq '.gossip.port = ["/ip4/0.0.0.0/tcp/'$gossipPort'"]' $goshimmerDir/config.json|sponge $goshimmerDir/config.json
    restartGoshimmer=true
fi